import axios from 'axios';

import buildPaginationQueryOpts from '@/shared/sort/sorts';

import { type ISubscriptionPlan } from '@/shared/model/subscription-plan.model';

const baseApiUrl = 'api/subscription-plans';

export default class SubscriptionPlanService {
  find(id: number): Promise<ISubscriptionPlan> {
    return new Promise<ISubscriptionPlan>((resolve, reject) => {
      axios
        .get(`${baseApiUrl}/${id}`)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  retrieve(paginationQuery?: any): Promise<any> {
    return new Promise<any>((resolve, reject) => {
      axios
        .get(`${baseApiUrl}?${buildPaginationQueryOpts(paginationQuery)}`)
        .then(res => {
          resolve(res);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  delete(id: number): Promise<any> {
    return new Promise<any>((resolve, reject) => {
      axios
        .delete(`${baseApiUrl}/${id}`)
        .then(res => {
          resolve(res);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  create(entity: ISubscriptionPlan): Promise<ISubscriptionPlan> {
    return new Promise<ISubscriptionPlan>((resolve, reject) => {
      axios
        .post(`${baseApiUrl}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  update(entity: ISubscriptionPlan): Promise<ISubscriptionPlan> {
    return new Promise<ISubscriptionPlan>((resolve, reject) => {
      axios
        .put(`${baseApiUrl}/${entity.id}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  partialUpdate(entity: ISubscriptionPlan): Promise<ISubscriptionPlan> {
    return new Promise<ISubscriptionPlan>((resolve, reject) => {
      axios
        .patch(`${baseApiUrl}/${entity.id}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }
}
